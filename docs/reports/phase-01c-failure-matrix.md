# MedStudy Atlas — Phase 1C Failure Matrix

- **Phase**: Vertical Slice 1C — Document Library & Secure Upload
- **Mode**: LOCAL-FIRST
- **Branch**: `phase/01c-documents`
- **Scope**: Security boundaries, storage isolation, quota enforcement, and failure scenarios.

---

## 1. Failure Scenario Matrix

| ID | Scenario | Expected Result | Test Layer | Test Name / Evidence | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **SEC-01** | Anonymous upload request | Denied with 42501 (Not authenticated) | Database (pgTAP) | `supabase/tests/database/03_documents_rls.sql` (`Anon: EXECUTE denied on request_document_upload`) | **PASS** |
| **SEC-02** | Anonymous direct document table mutation | Denied with 42501 (permission denied) | Database (pgTAP) | `supabase/tests/database/03_documents_rls.sql` (`Anon: SELECT/INSERT/UPDATE/DELETE denied on public.documents`) | **PASS** |
| **SEC-03** | Arbitrary direct Storage upload without reservation | Denied by storage RLS with 403 / AccessDenied | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`1. Direct unauthorized Storage upload is strictly denied`) | **PASS** |
| **SEC-04** | Arbitrary unreserved nested path upload | Denied by storage RLS | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`2. Arbitrary unreserved path upload is denied`) | **PASS** |
| **SEC-05** | Direct Storage DELETE by authenticated user | Denied; physical object retained | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`3. Direct Storage DELETE is strictly denied`) | **PASS** |
| **SEC-06** | Cross-user metadata read (Bob reads Alice's document) | Empty result; RLS enforces `auth.uid() = user_id` | Database (pgTAP) & Integration | `03_documents_rls.sql` (`Isolation: User B cannot SELECT User A documents`) & `storage-security.test.ts` (`20. Cross-user download authorization denied via application boundary (P0)`) | **PASS** |
| **SEC-07** | Cross-user signed download URL | Bob cannot obtain Alice's download signed URL; action / query rejects | Integration (Vitest) & Database (pgTAP) | `tests/integration/storage-security.test.ts` (`20. Cross-user download authorization denied via application boundary (P0)`) & `03_documents_rls.sql` | **PASS** |
| **SEC-08** | Cross-user delete / archive (Bob archives Alice's document) | Denied with 22023; row unaffected | Database (pgTAP) | `03_documents_rls.sql` (`Isolation: Cannot archive document belonging to another user`) | **PASS** |
| **SEC-09** | Cross-user subject association (Alice links Bob's subject) | Denied with 23503 (foreign key / subject ownership) | Database (pgTAP) | `03_documents_rls.sql` (`request_document_upload: Rejects linking another user subject`) | **PASS** |
| **SEC-10** | Fake PDF (.txt renamed to .pdf) | Container validation fails magic bytes (`%PDF-`); object removed; marked REJECTED | Integration & E2E | `storage-security.test.ts` (`11. Two-step cleanup lifecycle: UPLOADING → CLEANUP_PENDING → remove() → REJECTED (P0)`) & `document-management.spec.ts` | **PASS** |
| **SEC-11** | Spoofed MIME type (executable/HTML with PDF extension) | Magic byte inspection fails; rejected with INVALID_PDF_SIGNATURE; object removed | Integration & Unit | `storage-security.test.ts` (`11. Two-step cleanup lifecycle...`) & `tests/unit/documents.test.ts` | **PASS** |
| **SEC-12** | Oversized file (> 25 MB) | Rejected at request with 22023; schema validation fails | Database & Unit | `03_documents_rls.sql` (`Rejects size > 25 MB`) & `tests/unit/documents.test.ts` | **PASS** |
| **SEC-13** | Reserved-vs-actual size mismatch | Real uploaded object size verified; finalization rejects; physical blob deleted; marked REJECTED; quota released | Integration (Vitest) & Service | `tests/integration/storage-security.test.ts` (`24. Real size-mismatch storage integration: physical blob is removed, DB marked REJECTED, quota released (P1)`) & `03_documents_rls.sql` | **PASS** |
| **SEC-14** | Total quota bypass attempt (100 MB limit) | Counts UPLOADING (unexpired) & READY rows; rejects request exceeding 100 MB with 23514 | Database (pgTAP) | `03_documents_rls.sql` (`Quota: Rejects upload request that would exceed 100MB including UPLOADING rows`) | **PASS** |
| **SEC-15** | Active document count quota (max 10) | Rejects 11th active document with 23514 | Database (pgTAP) | `03_documents_rls.sql` (Active count constraint assertion) | **PASS** |
| **SEC-16** | Concurrent quota reservation | Serialized per user inside transaction via `pg_advisory_xact_lock`; exactly one succeeds, overflow fails with 23514 | Integration (Vitest) & Migration | `tests/integration/storage-security.test.ts` (`23. Real concurrent quota reservation: pg_advisory_xact_lock serializes parallel requests, exactly one succeeds, one fails (P1)`) & `20260920000000_documents_and_storage.sql` | **PASS** |
| **SEC-17** | Hostile filename (XSS payload `<img src=x onerror=alert('xss')>.pdf`) | Sanitized for metadata; rendered as literal text without execution | E2E (Playwright) & Unit | `tests/e2e/document-management.spec.ts` (`User-Content XSS Defense`) & `tests/unit/documents.test.ts` | **PASS** |
| **SEC-18** | Traversal-like filename (`../../../etc/passwd.pdf`) | `sanitizeFilename` strips directory traversal; system assigns canonical key | Unit (Vitest) | `tests/unit/documents.test.ts` (`strips unix/windows path traversal sequences`) | **PASS** |
| **SEC-19** | Abandoned upload (reservation made, no file uploaded) | Upload lease expires after 2 hours; lazy physical cleanup in `requestDocumentUpload` physically deletes any blob before transitioning to FAILED (`UPLOAD_TIMEOUT`) via `complete_document_cleanup_privileged`; slots/quota freed only after physical absence is confirmed | Integration (Vitest) & Database (pgTAP) | `tests/integration/storage-security.test.ts` (`14. Stale upload lazy cleanup physically deletes object before marking FAILED (P0)`) & `03_documents_rls.sql` | **PASS** |
| **SEC-20** | Storage deletion failure on archive or reject | Two-step cleanup lifecycle (`UPLOADING` -> `CLEANUP_PENDING` -> physical `remove()` -> `REJECTED`/`FAILED`). While in `CLEANUP_PENDING`, Storage RLS denies upload. If physical remove fails, status remains `CLEANUP_PENDING`, quota is NOT freed, and recoverable error is returned | Integration (Vitest) & Service | `tests/integration/storage-security.test.ts` (`13. If Storage remove() fails during cleanup...` & `19. Storage deletion failure on archive...`) & `src/modules/documents/service.ts` | **PASS** |
| **SEC-21** | Repeated finalization (idempotency) | Returns existing READY document cleanly without re-processing | Database (pgTAP) & Integration | `03_documents_rls.sql` (`Privileged finalize: Idempotent repeat call returns READY`) | **PASS** |
| **SEC-22** | Removal / archive retry (application idempotency) | Calling archive on already-archived document with absent blob returns success cleanly; no redundant deletion | Database (pgTAP) & Service | `03_documents_rls.sql` (`archive_document_privileged: Idempotent repeat call succeeds`) & `src/modules/documents/service.ts` | **PASS** |
| **SEC-23** | Signed upload token reuse after delete (reusable upload capability) | Replaced signed upload tokens with direct authenticated upload governed by reservation-backed Storage INSERT RLS policy on `storage.objects`. Storage RLS checks `status = 'UPLOADING'` in `public.documents`. Once the document transitions to `CLEANUP_PENDING`, `READY`, `REJECTED`, `FAILED`, or `archived_at IS NOT NULL`, or after 2 hours, Storage RLS immediately denies any further upload. Re-upload after delete is strictly denied | Integration (Vitest) & Database (pgTAP) | `tests/integration/storage-security.test.ts` (`4. Re-upload to SAME storage key after delete is DENIED by Storage RLS (proves token reuse impossible) (P0)`) & `03_documents_rls.sql` | **PASS** |
| **SEC-24** | Prior Phase 1A identity & auth regressions | User profile isolation, proxy claims, token handling pass | Database & E2E | `01_user_profiles_rls.sql` & `tests/e2e/auth-isolation.spec.ts` | **PASS** |
| **SEC-25** | Prior Phase 1B curriculum & onboarding regressions | Subject active unique index, composite FK, onboarding RPC pass | Database & E2E | `02_curriculum_rls.sql` & `tests/e2e/curriculum-management.spec.ts` | **PASS** |
| **SEC-26** | Direct authenticated invocation of privileged RPCs | Denied with 42501 (permission denied); only service_role can execute finalize, archive, start_cleanup, and complete_cleanup | Database (pgTAP) & Integration | `03_documents_rls.sql` & `storage-security.test.ts` (`18. Authenticated user cannot call any privileged RPC directly (P0)`) | **PASS** |
| **SEC-27** | Storage 5xx transient error during finalization | Transient 5xx returns recoverable error without rejecting document, deleting blob, or consuming quota | Integration (Vitest) & Service | `tests/integration/storage-security.test.ts` (`16. Storage 5xx error during finalizeDocumentUpload returns transient error without rejecting or deleting blob (P0)`) | **PASS** |
| **SEC-28** | Worst-case in-flight storage accounting | In-flight quota reserves 25 MB for active UPLOADING and CLEANUP_PENDING rows; 4 active rows (100 MB total) reject 5th upload even if declared 1 byte | Integration (Vitest) & Database (pgTAP) | `tests/integration/storage-security.test.ts` (`15. Worst-case in-flight quota: 4 active UPLOADING rows...`) & `03_documents_rls.sql` | **PASS** |
| **SEC-29** | Bounded range read for PDF container validation | HTTP Range request strictly requires HTTP 206, byteLength <= 5, and 5s timeout; HTTP 200 fails safely as transient error without downloading 25 MB | Integration (Vitest) & Service | `tests/integration/storage-security.test.ts` (`17. Bounded range read enforces HTTP 206, byteLength <= 5, and timeout; HTTP 200 fails safely (P1)`) | **PASS** |
| **SEC-30** | CLEANUP_PENDING recovery after transient deletion failure | Transient Storage deletion failure leaves row in CLEANUP_PENDING with quota reserved; subsequent requestDocumentUpload retries physical remove; on success, transitions to terminal state (FAILED/REJECTED), releases quota, and allows new upload | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`21. CLEANUP_PENDING recovery: transient remove failure leaves quota reserved, subsequent lazy retry succeeds, removes blob, transitions to FAILED, and frees quota for new upload (P0)`) | **PASS** |
| **SEC-31** | Archive / upload TOCTOU race condition | UPLOADING document is transitioned to CLEANUP_PENDING before physical Storage remove(); Storage INSERT RLS denies any concurrent upload attempt during physical deletion; archive completes safely | Integration (Vitest) & Service | `tests/integration/storage-security.test.ts` (`22. Adversarial archive/upload TOCTOU race: upload authority is closed (status=CLEANUP_PENDING) BEFORE physical deletion (P0)`) & `src/modules/documents/service.ts` | **PASS** |
| **SEC-32** | Storage 404 error classification (NoSuchKey vs NoSuchBucket vs ambiguous 404) | Conservative `isConfirmedObjectNotFoundError` classifies NoSuchKey as confirmed object absence; NoSuchBucket, 5xx, and generic ambiguous 404 fail closed, returning recoverable error without rejecting document or releasing quota | Integration (Vitest) & Unit | `tests/integration/storage-security.test.ts` (`26. Storage 404 classification in finalization...`) & `tests/unit/documents.test.ts` (`isConfirmedObjectNotFoundError`) | **PASS** |
| **SEC-33** | DB cleanup completion failure after successful physical deletion | Transient DB completion RPC failure preserves CLEANUP_PENDING and quota; subsequent recovery recognizes blob is already absent (NoSuchKey), completes DB cleanup, transitions to FAILED (CLEANUP_RECOVERED), and frees quota | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`25. DB completion failure after physical delete and subsequent recovery convergence (P0)`) | **PASS** |
| **SEC-34** | Storage 404 classification in archive idempotency | When checking already-archived row, NoSuchBucket returns recoverable storage error rather than claiming physical absence; only confirmed object absence (NoSuchKey) permits idempotent success | Integration (Vitest) | `tests/integration/storage-security.test.ts` (`27. Storage 404 classification in archive idempotency...`) | **PASS** |

---

## 2. Verification Summary
- **Database Test Suite (`supabase/tests/database/`)**: 190 pgTAP tests passing across 3 suites (62 in `03_documents_rls.sql`).
- **Unit & Integration Test Suite (`tests/unit/`, `tests/integration/`)**: 123 tests passing across 10 suites (including 27 Storage API integration tests).
- **End-to-End Suite (`tests/e2e/`)**: 17 Playwright tests passing across 5 suites.
- **Zero Secrets**: Automated audit confirms no secrets, tokens, or credentials committed.
